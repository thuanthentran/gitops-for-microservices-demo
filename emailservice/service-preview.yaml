apiVersion: v1
kind: Service
metadata:
  name: emailservice-preview
  labels:
    app: emailservice
spec:
  type: ClusterIP
  selector:
    app: emailservice
  ports:
    - name: grpc
      port: 5000
      protocol: TCP
      targetPort: 8080